{
  "name": "unbound-merch",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @store/web",
    "build": "npm run build --workspace @store/web",
    "start": "npm run start --workspace @store/web",
    "typecheck": "npm run typecheck --workspace @store/web"
  }
}
